/**
 * Use this class to create a new BuyExtraLifeButton.
 * Subclasses Button.
 *
 * Created by Lesleyjane J.
 */
public class BuyExtraLifeButton extends Button {
  
   /**
    * Create a new BuyExtraLifeButton by specifying the start x and y coordinates, the width and height,
    * size of text and String of text to be displayed on the button.
    */
   public BuyExtraLifeButton(float startX, float startY, int buttonWidth, int buttonHeight, int text_size, String buttonText) {
     super(startX, startY, buttonWidth, buttonHeight, text_size, buttonText);
   }
}
