/**
 * Use this class to create a new BuyJetpackButton.
 * Subclasses Button.
 *
 * Created by Lesleyjane J.
 */
public class BuyJetpackButton extends Button {
  
  /**
    * Create a new BuyJetpackButton by specifying the start x and y coordinates, the width and height,
    * size of text and String of text to be displayed on the button.
    */
   public BuyJetpackButton(float startX, float startY, int buttonWidth, int buttonHeight, int text_size, String buttonText) {
     super(startX, startY, buttonWidth, buttonHeight, text_size, buttonText);
   }
}
